services:
  - type: web
    name: rag-backend
    env: docker
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///./rag.db
    healthCheckPath: /api/health
    autoDeploy: true
